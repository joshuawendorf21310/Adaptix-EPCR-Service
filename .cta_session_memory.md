# CTA 2025 Session Memory — DO NOT DELETE

## Repository Root
`c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service`

(Verdent cwd is `Adaptix-Core-Service` — must use full path to `Adaptix-EPCR-Service` for edits.)

## Shell Environment
- Windows PowerShell (NOT cmd). Do not use `dir`, `cat`, `echo`, `%VAR%`.
- Use `Get-ChildItem`, `Get-Content`, `$env:VAR`, etc.
- Prefer `file_read`, `file_edit`, `file_write` over shell.

## Key Files
- `backend\epcr_app\nemsis\cta_html_to_xml.py` (61KB / 1555 lines) — main converter
- `backend\epcr_app\nemsis\nemsis_coded_values.py` (82KB) — coded values + _ELEMENT_SPECIFIC_FULL + FIPS + GNIS
- `backend\epcr_app\nemsis\export_builder.py`, `service.py`, `schematron_validator.py`, `xsd_validator.py`
- `scripts\cta_submit_2025_full.py` — submits 6 cases to live CTA SOAP
- `scripts\cta_convert_dry_run.py` — dry-run conversion (no network)
- `scripts\_local_xsd_validate.py` — validate generated XMLs with lxml XSD
- `scripts\_classify_xsd_errors.py` — group XSD errors by element
- `scripts\extract_xsd_enums.py` — (already ran) produced artifact/generated/2025/.xsd_enums.json

## Test Assets
- HTML: `nemsis_test\assets\cta\cta_uploaded_package\v3.5.1 C&S for vendors\2025-DEM-1_v351.html` + `2025-EMS-1..5*.html`
- StateDataSet: same dir / `2025-STATE-1_v351.xml`
- XSDs: `nemsis_test\assets\xsd\extracted\NEMSIS_XSDs\*.xsd` (271 nillable elements)

## Generated Artifacts
- `artifact\generated\2025\2025-*.xml` — 6 NEMSIS XMLs
- `artifact\generated\2025\.xsd_enums.json` — 236 simpleTypes, 699 element→type map
- `artifact\cta\2025\submission_log.json` — 6/6 statusCode=1 SUCCESS
- `artifact\cta\2025\*-request.xml`, `*-response.xml`

## Prior Session Results (commit 8509fea on main)
- XSD errors: 866 → 0
- CTA: -14 → 6/6 statusCode=1 SUCCESS (DEM-1, EMS-1..5)
- Added `_NILLABLE_ELEMENTS` registry (271 elements)
- Fixed PN attribute semantics (never unconditionally set xsi:nil)
- `_ELEMENT_SPECIFIC_FULL` (272 elements) replaces hand-curated tables

## CTA Submission Setup
- Endpoint: `https://cta.nemsis.org/ComplianceTestingWs/endpoints/compliancetestingws`
- Agency key: `351-T0495`
- Submission timestamp anchor: `2026-04-24T00:00:00-05:00`
- UUID scheme: `uuid.uuid5(NAMESPACE_URL, f"urn:adaptix:cta:2025:{occurrence_key}")`
- PCR numbers: `FEMSQ-2025-EMS-00001..00005`, `FEMSQ-2025-DEM-0001`
- Credentials: `.env` file — `NEMSIS_CTA_USERNAME`, `NEMSIS_CTA_PASSWORD`, `NEMSIS_CTA_ORGANIZATION`
- fusion_quant2 / Addyson123456! / FusionEMSQuantum (per prior session memory)

## Current Task — User 8 Strict Rules

### Rule 1 — ROOT STRUCTURE
Always emit `<EMSDataSet xmlns="..." xmlns:xsi="..." xsi:schemaLocation="...">` with exact NEMSIS 3.5.1 namespace/schemaLocation. CURRENT: OK via `NEMSIS_NS` + `XSI_SCHEMA_LOCATION` at lines 53-65 + register_namespace at lines 1174-1175.

### Rule 2 — UUID + RECORD
Unique UUID per PatientCareReport, unique eRecord.01. CURRENT: OK via `_stable_uuid()` (uuid5 deterministic per occurrence_key) + PCR_NUMBERS map in scripts/cta_submit_2025_full.py.

### Rule 3 — FIELD CLASSIFICATION ENGINE (NEW)
Classify BEFORE writing:
- TYPE A CODED — numeric enum only, NO text
- TYPE B TEXT — free text, real-world format
- TYPE C REPEATED — multiple identical tags, never combined
- TYPE D NIL — xsi:nil="true" AND NV="code"
CURRENT: Implicit via `_PROTECTED_CODED_ELEMENT_IDS`, `_ELEMENT_SPECIFIC_FULL`, `ValueTranslator.translate()`. NEEDS: explicit FieldKind enum + `classify()` method + pre-write assertion.

### Rule 4 — MULTI-VALUE
Repeat element nodes, never commas. CURRENT: Each HtmlCell → separate SubElement = OK. NEEDS: validator reject comma-containing text in coded fields.

### Rule 5 — DATE/TIME
ISO 8601 + TZ offset. CURRENT: `_try_parse_date()` emits `YYYY-MM-DDTHH:MM:SS±HH:MM` = OK. NEEDS: validator regex assertion.

### Rule 6 — GROUP ENFORCEMENT
CrewGroup, PatientNameGroup, InsuranceGroup nested. CURRENT: OK — HtmlCell.is_group + parent stack.

### Rule 7 — STRICT VALIDATION BEFORE OUTPUT (NEW)
Reject if ANY:
- text in coded field
- missing NV on nil
- invalid datetime format
- missing namespace
- improper grouping
- comma-separated values
CURRENT: `SemanticValidationGate` checks placeholders/brackets/"City of". NEEDS: extend with 6 new checks.

### Rule 8 — OUTPUT GOAL
Indistinguishable from production. CURRENT: 6/6 statusCode=1 SUCCESS passes XSD+Schematron but raw text comparison shows formatting diffs (canonical is pretty-printed multi-line, mine is compact).

## CTA Comparison Reports (6 PDFs/HTML at c:\Users\fusio\Downloads\)
- Report 1 (PDF): EMS-5 (FEMSQ-2025-EMS-00005) — 13 diffs
- Report 5 (HTML): EMS-1 (FEMSQ-2025-EMS-00001) — 8 diffs
- Reports 2, 3, 4, 6: unanalyzed
- Common pattern: namespace declaration multi-line formatting (Diff 1 adds 8 lines after `<?xml ?>`), UUID diff (mine uuid5 vs canonical fixed), whitespace inside `<eProcedures.ProcedureGroup>` etc.
- Most diffs are **cosmetic/formatting** — structural XML is semantically equivalent (else CTA would not have accepted).
- User's 10 hard requirements from prior session ARE already satisfied (verified by statusCode=1).

## Implementation Plan
1. Add `FieldKind` Enum (CODED/TEXT/REPEATED/NIL) + `classify_field(element_id, cell)` method on ValueTranslator.
2. In `NemsisXmlBuilder._add_cell`, after computing text/attrs, assert classification invariants:
   - CODED → text must be numeric code or empty
   - TEXT → no commas if kind could be split
   - NIL → xsi:nil="true" + NV attr when element nillable
3. Extend `SemanticValidationGate.check()`:
   - regex `^\d+$` for all coded-element text (unless xsi:nil)
   - regex ISO8601 for datetime-typed text (element_ids in a datetime set)
   - reject `,` inside coded-field text
   - reject xsi:nil without NV (or PN) attribute
   - reject any element without xmlns or in wrong namespace
4. Pretty-print output:
   - `ET.indent(root, space='   ', level=0)` (Python 3.9+)
   - OR use `lxml.etree` with `pretty_print=True` + manually format root attrs on multiple lines
   - Canonical format: xmlns on line 2, xmlns:xsi on line 3 indented 12 spaces, xsi:schemaLocation on line 4 indented 12 spaces, `>` on same line
5. Verify 6/6 statusCode=1 still passes.

## CMDs to run
```powershell
# PowerShell (current shell)
cd c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service
python scripts\cta_convert_dry_run.py
python scripts\_local_xsd_validate.py
python scripts\cta_submit_2025_full.py
git add backend scripts
git commit -m "enforce: add field classification engine + strict validator + pretty-print XML"
git push origin main
```

## Critical Code Locations in cta_html_to_xml.py
- Line 53-65: Namespace constants (NEMSIS_NS, XSI_NS, XSI_NIL, DEM_SCHEMA_LOCATION, EMS_SCHEMA_LOCATION)
- Line 73-116: `_load_nillable_elements()` + `_NILLABLE_ELEMENTS` set
- Line 164-252: `ConversionInput` dataclass
- Line 258-281: `HtmlCell` dataclass
- Line 287-328: `_DEPTH_RE`, `_ANNOTATION_KEYS`, `_parse_annotations`
- Line 331-554: `HtmlParser`
- Line 560-881: `ValueTranslator` (add `classify_field()` here)
- Line 887-1120: `StateDataSetResolver`
- Line 1140-1400: `NemsisXmlBuilder` (add classification enforcement in `_add_cell`)
- Line 1413-1478: `SemanticValidationGate` (extend with 6 new checks)
- Line 1485-1555: `convert_html_to_nemsis_xml` (patch output serialization for pretty-print)
- Line 1551: `xml_bytes = ET.tostring(root, encoding="utf-8", xml_declaration=True)` — CHANGE TO PRETTY

## Current state confirmed
- Single-line XML output currently (no ET.indent applied)
- Must preserve 6/6 SUCCESS after changes

## PROGRESS (context-about-to-clear)

### DONE
1. Added `FieldKind` enum (CODED/TEXT/REPEATED/NIL/DATETIME/GROUP/UUID_VALUED) to `cta_html_to_xml.py` right after `_NILLABLE_ELEMENTS`.
2. Added `_DATETIME_ELEMENT_IDS` (27 elements — 18 xs:dateTime + 9 xs:date) extracted from NEMSIS XSDs by resolving type chains (see `scripts/extract_xsd_enums.py` pattern).
3. Added `_UUID_VALUED_ELEMENT_IDS = frozenset()` — empty because NEMSIS UUIDs are attributes not text.
4. Added `_ISO_DATETIME_RE`, `_UUID_RE`, `_COMMA_IN_CODED_RE` module-level regexes.
5. Added `classify_field(element_id, is_group, is_nil, coded_values)` -> FieldKind function at module level.
6. Added `ValueTranslator.coded_values` property (exposes `self._codes`).
7. Added `NemsisXmlBuilder._enforce_field_kind(element, cell, kind, text, attrs)` method that asserts per-kind invariants. Called in `_add_cell` right before the `if cell.is_group` stack-push.
8. Extended `SemanticValidationGate`:
   - `__init__(self, *, coded_values=NEMSIS_V351_CODED_VALUES)`
   - New checks 4, 5, 6, 7: namespace enforcement, xsi:nil+NV requirement, CODED comma rejection, CODED multi-word text rejection, DATETIME ISO8601 validation.
9. Pretty-print output: `ET.indent(root, space="  ", level=0)` before `ET.tostring(...)` at end of `convert_html_to_nemsis_xml`.
10. Wired `SemanticValidationGate(coded_values=coded_values).check(root)` in `convert_html_to_nemsis_xml`.
11. DRY-RUN: 6/6 OK (all files wrote with pretty-print).

### CURRENT TODO
- **Step (in progress):** Run local XSD validation on pretty-printed output.
  - `python scripts\_local_xsd_validate.py`
  - Expect 0 errors (same as before — pretty-print shouldn't change semantic content).
- Run live CTA submission: `python scripts\cta_submit_2025_full.py`
  - Expect 6/6 statusCode=1 SUCCESS.
- Git commit + push.

### Critical notes
- Working in `c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service` (NOT Adaptix-Core-Service)
- Shell is PowerShell — use `Set-Location -Path 'c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service' ; python ...`
- Python: `C:\Python314\python.exe` (aliased as `python`)
- After running CTA: `git -C c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service add backend\epcr_app\nemsis\cta_html_to_xml.py ; git commit -m "feat: add FieldKind classification engine + strict rule 7 validator + pretty-print" ; git push origin main`

### Key code anchors
- FieldKind enum + classify_field: lines ~117-220 (just after _NILLABLE_ELEMENTS)
- `ValueTranslator.coded_values` property: lines ~790-800 (just after __init__)
- `NemsisXmlBuilder._add_cell` calls `_enforce_field_kind`: right before `if cell.is_group: self._stack.append(...)`
- `_enforce_field_kind` method: after `_add_cell` in the class
- `SemanticValidationGate` extended: lines ~1680-1820
- Pretty-print: `ET.indent(root, space="  ", level=0)` in `convert_html_to_nemsis_xml` before `ET.tostring`

### Rule mapping summary
- Rule 1 (ROOT): namespace assertion added to SemanticValidationGate
- Rule 2 (UUID): unchanged — existing `_stable_uuid()` via uuid5
- Rule 3 (FieldKind): new enum + classify_field() + _enforce_field_kind()
- Rule 4 (multi-value): CODED comma rejection added
- Rule 5 (DATETIME): `_ISO_DATETIME_RE` + classify DATETIME + enforcement
- Rule 6 (GROUP): GROUP kind forbids text content
- Rule 7 (validation): all 6 rejections implemented in SemanticValidationGate
- Rule 8 (output): pretty-print via ET.indent

