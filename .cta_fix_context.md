# CTA 2025 Fix — URGENT 15-MIN CHECKPOINT

## STATUS
- 612 → 154 errors after XSD auto-generator (nemsis_coded_values.py)
- User has 15 min to submit

## DONE THIS SESSION
1. `scripts/cta_submit_2025_full.py` lines 99-102: removed `DEM_REFERENCES_GLOBAL` blanket overrides (was forcing "FAC-OKALOOSA-001" / "351-T0495") — now empty dict.
2. `backend/epcr_app/nemsis/cta_html_to_xml.py`:
   - `_STATE_ELEMENT_IDS`: added `dPersonnel.06`, `dPersonnel.22`, `eHistory.06`, `ePayment.28`
   - `_COUNTRY_ELEMENT_IDS`: added `dLocation.11`, `dPersonnel.08`, `eDisposition.08`
   - `StateDataSetResolver`: 
     - Added facility code indexing (sFacility.03 also keys `_facility_groups`)
     - Added `_current_destination` and `set_destination_context()`
     - Extended `resolve()` for `eDisposition.NN` (.03→.07, .04→.08, .05→.09, .06→.11, .07→.10, .08→.12, .09→.13, .10→.14)
3. `backend/epcr_app/nemsis/nemsis_coded_values.py`: auto-generator from `.xsd_enums.json` already wired — 272 elements get label→code + "{code} - {label}" aliases.

## REMAINING EDITS REQUIRED

### A — Builder must set destination context on eDisposition.01/.02
File: `backend/epcr_app/nemsis/cta_html_to_xml.py` around line 1187 (`_add_cell`).

After the existing dFacility.02 context-setting block, add:
```python
if (
    cell.element_id in ("eDisposition.01", "eDisposition.02")
    and not cell.state_ref
    and not cell.dem_ref
    and text is not None
):
    self._state_resolver.set_destination_context(text)
```

Also clear destination context on group boundaries (e.g. `eDisposition` group root).

### B — Suppress empty-string elements
In `_add_cell` after `_extract_value` — if text is empty/whitespace AND the cell has NV attribute, still emit. Otherwise skip emission. Need to see the full `_add_cell` body to wire properly.

### C — Handle eVitals.901 (custom element not in schema)
Drop emission of `eVitals.901` entirely (it is custom data and not in NEMSIS v3.5.1 XSD). The HTML parser might be creating these. Best fix: in builder `_add_cell`, if cell element_id contains `.901` suffix, skip emission.

### D — `[Custom Value] COVID-19` on `dPersonnel.18`
The element is an enum-only type. Options:
- Skip emission
- Replace with NV code (like `7701005` = "Not Recorded" or "Not Applicable" per xs:pattern allowed NV)

### E — `dVehicle.13@DistanceUnit=2516081`
Attribute translator is returning wrong code. Need to fix in `translate_attribute` of ValueTranslator for DistanceUnit.

## VALIDATION COMMANDS
```
cd c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service
python scripts\cta_convert_dry_run.py
python scripts\_local_xsd_validate.py
```

## SUBMISSION
```
python scripts\cta_submit_2025_full.py
```
Creds: fusion_quant2 / Addyson123456! / FusionEMSQuantum
DEM=62, EMS=61

## PATHS
- Repo: `c:\Users\fusio\Desktop\workspace\Adaptix-EPCR-Service`
- Generated XMLs: `artifact/generated/2025/*.xml`
- XSD enums: `artifact/generated/2025/.xsd_enums.json`
- Error classification: `artifact/generated/2025/.xsd_errors_classified.json`
- NEMSIS XSDs: `nemsis_test/assets/xsd/extracted/NEMSIS_XSDs/`
- HTML sources: `nemsis_test/assets/cta/cta_uploaded_package/v3.5.1 C&S for vendors/`

## KEY LINES (cta_html_to_xml.py)
- `_STATE_ELEMENT_IDS` line ~524
- `_COUNTRY_ELEMENT_IDS` line ~540
- `_COUNTY_ELEMENT_IDS` line ~551
- `_CITY_ELEMENT_IDS` line ~561
- `_PROTECTED_CODED_ELEMENT_IDS` line ~573
- `class StateDataSetResolver` line ~826
- `set_facility_context` line ~934
- `set_destination_context` NEW line ~945
- `resolve` line ~956
- `class NemsisXmlBuilder` line ~1043
- `_extract_value` line ~1170
- `_add_cell` line ~1203
- `class SemanticValidationGate` line ~1254+

## NEXT IMMEDIATE STEP
Read `_add_cell` body (current state) to see where `text = self._extract_value(cell)` is called, then:
1. Add destination-context set block there
2. Add empty-string guard
3. Add `.901` drop guard

Then re-run dry-run + local validation. If clean, submit.
